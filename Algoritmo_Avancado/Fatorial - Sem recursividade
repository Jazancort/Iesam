#include <stdio.h>
 
int main() {
  int n, i;
  long fat=1;

  printf("Escreva qualquer numero: ");
  scanf("%d", &n);
  
  if(n<0)
    printf("Tente um numero positivo");
    else{
  for(i=1; i<=n; i++)
  fat= fat*i;
  printf("Fatorial= %ld", fat);
    }
}
